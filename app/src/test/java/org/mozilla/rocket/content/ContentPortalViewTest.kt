package org.mozilla.rocket.content

import org.junit.Test

class ContentPortalViewTest {

    @Test
    fun `Swipe up or tap the arrow to show content portal panel`() {
    }

    @Test
    fun `Load 30 the latest news when open the panel`() {
    }

    @Test
    fun `Load more automatically at the bottom of the list`() {
    }

    @Test
    fun `Open a new tab & back to previous page`() {
    }

    @Test
    fun `(No any content in cache)`() {
    }

    @Test
    fun `Open panel if having content in cache & check new news`() {
    }

    @Test
    fun `Refresh the list automatically (Clear old news and show new news)`() {
    }

    @Test
    fun `When at the top of the list, swipe down   the list then show half panel`() {
    }

    @Test
    fun `No support half panel when there is no content`() {
    }

    @Test
    fun `Press back then close the panel`() {
    }
}